# Base URL of the AmanGuard Spring Boot backend
VITE_API_BASE_URL=http://localhost:8080/api

# While true (default), API calls fall back to mock data automatically.
# Set to false once the backend (/analyze, /freeze, /cases/active) is live.
VITE_USE_MOCKS=true
